
    <div class="dcf-grid dcf-col-gap-vw dcf-flex-wrap">
        <div class="dcf-col-50% dcf-d-flex dcf-jc-center dcf-ai-center">
            <h1 class="dcf-txt-h1 dcf-txt-center" id="Title">Kiewit Garage Status</h1>
        </div>

        <div class="dcf-col-50% dcf-flex dcf-jc-center">
            <h3 class="dcf-txt-h3 dcf-txt-center">Weekly Forecast</h3>
                <div class="dcf-col-100% dcf-w-100% dcf-jc-center" style="margin-top:15px">
                    <table class="dcf-table dcf-table-bordered dcf-w-100%">
                    <thead>
                        <tr>
                        <th></th>
                        <% forecasts_by_day.each_key do |day| %>
                            <th><%= day %></th>
                        <% end %>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                        <th scope="row">Forecast</th>
                        <% forecasts_by_day.each_value do |forecast| %>
                            <td><%= forecast.nil? ? "N/A" : forecast %></td>
                        <% end %>
                        </tr>
                        <tr>
                        <th scope="row">Change</th>
                        <% forecasts_by_day.each_key do |day| %>
                            <% change = changes_by_day[day] %>
                            <td style="color: <%= change.nil? ? 'black' : (change > 0 ? 'green' : (change < 0 ? 'red' : 'gray')) %>">
                            <%= change.nil? ? "N/A" : "#{change > 0 ? '+' : ''}#{change}" %>
                            </td>
                        <% end %>
                        </tr>
                    </tbody>
                    </table>
                </div>    
        </div>    
    </div>

    <div class="dcf-box dcf-border-all dcf-p-6 dcf-mb-4">
        <h2 class="dcf-txt-center">A beautiful histogram here</h2>
        <p>
            This box will be changed to a histogram at a later time
        </p>


    </div>

    <div class="dcf-grid dcf-col-gap-vw dcf-flex-wrap">

        <%# left side of the page#%>
        <div class="dcf-col-50% dcf-flex dcf-jc-center">
            <h1 class="dcf-txt-h3 dcf-txt-center" id="timeless_title">Timeless Event Potentials</h1>
                <div class="dcf-col-100% dcf-w-100%" style="margin-top:15px">
                    <table class="dcf-table dcf-table-bordered dcf-w-100%" aria-describedby="timeless_potentials">
                        <thead>
                            <tr>
                                <th scope="col">Event</th>
                                <th class="dcf-txt-center" scope="col">Potentials</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>New User Orientation</td>
                                <td class="dcf-txt-center"><%= orientation_potentials %> </td>
                            </tr>
                            <% timeless_events.each do |event| %>
                                <tr>
                                    <td><%= event[:title] %></td>
                                    <td class="dcf-txt-center"><%= event[:potential_walkins] %></td>
                                </tr>
                            <% end %>    
                        </tbody>
                    </table>
                </div>
        </div>
            

        <%# right side of the page  dcf-grid dcf-col-gap-vw dcf-flex-wrap  #%>
        <div class="dcf-col-50% dcf-flex dcf-jc-center">
            <h1 class="dcf-txt-h3 dcf-txt-center" id="loto_title">Locked Out / Tag Out Machines</h1>

            <div class="dcf-col-gap-vw" style="margin-top:15px">
                <table class="dcf-table dcf-table-bordered dcf-w-100%" aria-describedby="num_down_machines">
                    <thead>
                        <tr>
                            <th class="dcf-txt-center">Number of Locked Out / Tag Out Machines</th>
                            <th class="dcf-txt-center"><%= lockout_count %></th>
                        </tr>
                    </thead>
                </table>
            </div>
                            
            <div class="dcf-col-100% dcf-w-100% dcf-flex-wrap">
                <div class="dcf-col-67%-center dcf-felx-wrap">
                    <h4 class="dcf-txt-h4 dcf-txt-center">Currently Locked Out Equipment</h4>
                    <table class="dcf-table dcf-table-bordered dcf-w-100%" aria-describedby="Down-Machines">
                        <thead>
                            <tr>
                                <th>Machine Name</th>
                                <th>Description</th>
                                <th>Start Date</th>
                                <th>Scheduled Release Date</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% lockouts.each do |lockout| %>
                                <tr>
                                    <td><%= lockout.resource.name if lockout.resource %></td>
                                    <td><%= lockout.description %></td>
                                    <td><%= lockout.started_on.strftime('%Y-%m-%d %H:%M') if lockout.started_on %></td>
                                    <td><%= lockout.released_on.strftime('%Y-%m-%d %H:%M') if lockout.released_on %></td>
                                </tr>
                            <% end %>
                        </tbody>
                    </table>
                    <h4 class="dcf-txt-h4 dcf-txt-center">Upcoming Scheduled Lockouts</h4>
                    <table class="dcf-table dcf-table-bordered dcf-w-100%" aria-describedby="Upcoming-Machines">
                        <thead>
                            <tr>
                                <th>Machine Name</th>
                                <th>Description</th>
                                <th>Scheduled Start Date</th>
                                <th>Scheduled Release Date</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% upcoming_lockouts.each do |lockout| %>
                                <tr>
                                    <td><%= lockout.resource.name if lockout.resource %></td>
                                    <td><%= lockout.description %></td>
                                    <td><%= lockout.started_on.strftime('%Y-%m-%d %H:%M') if lockout.started_on %></td>
                                    <td><%= lockout.released_on.strftime('%Y-%m-%d %H:%M') if lockout.released_on %></td>
                                </tr>
                            <% end %>
                        </tbody>
                    </table>
                </div>
            </div>
        
        </div>
    </div>

